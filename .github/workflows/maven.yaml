name: Build Maven Project with Java 8
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
    - name: Set up JDK
      uses: actions/setup-java@v2
      with:
          distribution: 'adopt'
          java-version: '8'

    - name: Build with Maven
      run: mvn clean install     

    - name: Transfer build file to remote server1
      run: scp -i /root/.ssh/viki_rsa target/*.war ${{ secrets.USERNAME }}@${{ secrets.HOSTNAME }}:/home/vvdn/


    - name: Set up SSH to remote 1
      uses: appleboy/ssh-action@v0.1.10
      with:
          host: ${{ secrets.HOSTNAME }}
          port: ${{secrets.PORT}}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.KEY }}
          command_timeout: 30m
          script: | 
            ./home/vvdn/Documents/apache-tomcat-9.0.73/shutdown.sh
            cp /home/vvdn/*.war /home/vvdn/Documents/apache-tomcat-9.0.73/webapps/
            ./home/vvdn/Documents/apache-tomcat-9.0.73/startup.sh
            tail -2f ./home/vvdn/Documents/apache-tomcat-9.0.73/logs/catalina.out
            
   

    
            
  

    
        

  
